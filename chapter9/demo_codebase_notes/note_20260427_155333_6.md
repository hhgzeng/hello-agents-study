---
id: note_20260427_155333_6
title: 📅 本周重构计划 Week 1 — demo_codebase 代码质量提升
type: task_state
tags: ["refactoring", "weekly_plan", "demo_codebase", "week1"]
created_at: 2026-04-27T15:53:33.760837
updated_at: 2026-04-27T15:53:33.760840
---

# 📅 本周重构计划 Week 1 — demo_codebase 代码质量提升

# 📅 本周重构计划 Week 1 — demo_codebase 代码质量提升

> **计划周期**: 5 个工作日 | **总预算工时**: ~12 小时 | **状态**: 🟢 待启动

---

## 一、🎯 总体目标

| 目标 | 量化指标 | 优先级 |
|------|---------|--------|
| **G1: 解决"一碰就崩"的安全问题** | api_client 错误处理覆盖率 0% → 100% | 🔴 P0 |
| **G2: 建立回归安全网** | 测试覆盖率 0% → 核心模块 ≥ 80% | 🔴 P0 |
| **G3: 清理 13 个历史 TODO** | TODO 数量 13 → ≤ 3 | 🟡 P1 |
| **G4: 完善管线功能完整性** | process_data() 完整集成 5/5 个步骤 | 🟡 P1 |
| **G5: 工程化达标** | 依赖管理 + 日志 + .gitignore 到位 | 🟢 P2 |

---

## 二、📋 具体任务清单（9 大任务，按天编排）

### 📅 第 1 天：基础设施搭建 + 高风险模块开工

#### 任务 1.1 — 创建 requirements.txt（~15 分钟）
```
# 内容
requests>=2.28.0
pandas>=1.5.0
# 开发依赖
pytest>=7.0.0
pytest-cov>=4.0.0
```
- **验收标准**: `pip install -r requirements.txt` 成功安装

#### 任务 1.2 — 搭建 pytest 测试框架（~30 分钟）
```
tests/
├── conftest.py          # 测试夹具（API mock、示例数据）
├── test_imports.py      # 冒烟测试：验证所有模块可导入
├── test_api_client.py   # [占位]
├── test_data_processor.py # [占位]
├── test_models.py       # [占位]
└── test_utils.py        # [占位]
```
- **验收标准**: `pytest tests/` 绿色通过，至少 1 个冒烟测试

#### 任务 1.3 — api_client.py 重构：第一步（~1.5 小时）
- [ ] 定义自定义异常体系（`APIClientError`, `APINetworkError`, `APITimeoutError`, `APIHTTPError`, `APIResponseError`）
- [ ] 提取 `_request()` 私有方法（统一 4 个 HTTP 方法的公共逻辑）
- [ ] 为所有请求添加 `timeout` 参数（默认 30 秒）
- [ ] 编写 `test_api_client.py` 基础测试（正常请求、超时、网络错误）
- **验收标准**: 4 个 HTTP 方法均通过 `_request()` 转发，全部异常场景有测试覆盖

---

### 📅 第 2 天：高风险模块收尾 + 中间模块加固

#### 任务 1.3（续）— api_client.py 重构：完善（~1.5 小时）
- [ ] 实现重试机制（指数退避，默认 3 次重试）
- [ ] 添加 `logging` 日志（请求/响应/异常分级记录）
- [ ] 完善 docstring 标注异常信息
- [ ] 异常场景测试覆盖率达 90%+
- **验收标准**: api_client.py 全部 P0/P1 任务完成，测试通过

#### 任务 2.1 — data_processor.py 加固（~2 小时）
- [ ] `transform_data()`：增加 `'date'` 列存在性检查，缺失时抛出明确错误
- [ ] `clean_data()`：增加空 DataFrame 保护，增加 `subset` 参数支持按列去重
- [ ] `process_data()`：增加输入类型校验（非列表、空列表的处理）
- [ ] `export_data()`：用 `logging.info()` 替换 `print()`
- [ ] 编写 `test_data_processor.py`（正常流程 + 边界场景）
- **验收标准**: 5 个函数均有输入保护，测试覆盖正常 + 异常路径

---

### 📅 第 3 天：功能完善 — 管线完整化

#### 任务 3.1 — process_data() 管线完整化（~2.5 小时）
- [ ] 重构 `process_data()` 签名：增加 `aggregate_by`, `export_path` 可选参数
- [ ] 实现管线内部自动判断：若有 `aggregate_by` 则执行聚合，若有 `export_path` 则导出
- [ ] 解决 `aggregate_data()` 聚合规则硬编码：允许用户自定义聚合字典
- [ ] 解决 `transform_data()` 列名硬编码：允许用户指定日期列名
- [ ] 更新测试覆盖完整管线流程
- **验收标准**: 一次调用 `process_data(data, aggregate_by='category', export_path='out.csv')` 即可完成全流程

#### 任务 3.2 — models.py TODO 清理（~1 小时）
- [ ] `Product`: 实现 `validate_discount()`（折扣值校验 0~100）
- [ ] `Order`: 实现 `calculate_tax()`（默认税率可配）
- [ ] `Transaction`: 实现 `process_refund()`（退款状态机逻辑）
- [ ] 为所有 4 个数据类添加 `__repr__()` 方法
- [ ] 编写 `test_models.py`
- **验收标准**: 4 个 TODO 均实现，所有数据类可打印可测试

---

### 📅 第 4 天：工具模块完善 + 全库日志

#### 任务 3.3 — utils.py 边界处理完善（~1 小时）
- [ ] `load_config()`：文件不存在时抛出 `FileNotFoundError`（含路径信息）
- [ ] `validate_email()`：使用正则（`r'^[\w\.-]+@[\w\.-]+\.\w+$'`）替换简单字符串检查
- [ ] `format_size()`：处理非 int/float 输入（类型转换 + 异常安全）
- [ ] 编写 `test_utils.py`（覆盖正常 + 边界 + 异常输入）
- **验收标准**: 3 个函数边界处理完善，测试覆盖所有场景

#### 任务 4.1 — 全库 logging 替换（~30 分钟）
- [ ] 每个模块添加 `logger = logging.getLogger(__name__)`
- [ ] 替换 `export_data()` 中的 `print()` 为 `logging.info()`
- [ ] 在 `__init__.py` 中提供 `setup_logging()` 便捷配置入口
- **验收标准**: 全库无 `print()` 残留，所有日志通过 logging 体系输出

---

### 📅 第 5 天：工程化收尾 + 整体验证

#### 任务 4.2 — 工程化配置补充（~30 分钟）
- [ ] 创建 `.gitignore`（排除 `__pycache__/`, `*.pyc`, `*.csv`, `.pytest_cache/`）
- [ ] 创建 `pyproject.toml`（项目元信息 + 构建配置）
- [ ] 添加 `tox.ini` 或 `.flake8` 代码风格配置（可选）
- **验收标准**: `git status` 显示干净的工作区

#### 任务 5.1 — 整体回归验证（~1 小时）
- [ ] 全量运行 `pytest tests/ --cov=. --cov-report=term-missing`
- [ ] 验收覆盖率目标：核心模块 ≥ 80%
- [ ] 最终检查：确认 13 个 TODO 已清理至 ≤ 3 个
- [ ] 记录本次重构的结论文档
- **验收标准**: 全部测试绿色通过，覆盖率达标

---

## 三、🗓️ 时间安排总表

| 天数 | 日期 | 任务 | 预估工时 | 交付物 |
|------|------|------|---------|--------|
| **Day 1** | 周一 | 1.1 requirements.txt（15min） | **~2 小时** | ✅ requirements.txt |
| | | 1.2 测试框架搭建（30min） | | ✅ tests/ 目录 + 冒烟测试 |
| | | 1.3 api_client 重构·上半（75min） | | ✅ 异常体系 + _request() 方法 |
| **Day 2** | 周二 | 1.3 api_client 重构·下半（90min） | **~3.5 小时** | ✅ api_client.py 完整重写 |
| | | 2.1 data_processor 加固（120min） | | ✅ data_processor.py 输入防护 |
| **Day 3** | 周三 | 3.1 管线完整化（150min） | **~4 小时** | ✅ process_data() 全链路 |
| | | 3.2 models TODO 清理（60min） | | ✅ models.py 4 个 TODO 解决 |
| **Day 4** | 周四 | 3.3 utils 完善（60min） | **~1.5 小时** | ✅ utils.py 边界加固 |
| | | 4.1 logging 替换（30min） | | ✅ 全库零 print |
| **Day 5** | 周五 | 4.2 工程化配置（30min） | **~1 小时** | ✅ .gitignore + pyproject.toml |
| | | 5.1 回归验证（60min） | | ✅ 最终验收报告 |
| | **总计** | **9 项任务** | **~12 小时** | **代码质量 3/5 → 4.5/5** |

---

## 四、⚠️ 风险与应对

### 高风险（发生概率 ≥ 30%，影响大）

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| **R1: models.py 业务逻辑不确定**（折扣/税费/退款规则需与需求方确认） | 🟡 中 | 🟡 中 | 任务 3.2 预留弹性：先实现基础逻辑，标记可配置参数，待确认后调整 |
| **R2: 重构引入新 bug** | 🟡 中 | 🔴 高 | **测试先行（TDD）**：任务 1.2 先建测试框架，每个改进前先写测试验证旧行为 |
| **R3: 时间超预算**（12h 为净编码，未含 review/调试） | 🟡 中 | 🟡 中 | 按优先级裁剪：P2 任务（4.1/4.2）可作为缓冲，优先保障 P0 任务完成 |

### 低风险（发生概率 < 20%，影响可控）

| 风险 | 应对 |
|------|------|
| **R4: 依赖库兼容性问题**（requests/pandas 新版本 API 变化） | 在 requirements.txt 中锁定大版本号；测试先行可快速发现 |
| **R5: 任务执行人临时不可用** | 每个任务 ≤ 2.5 小时，粒度小，可随时暂停/交接 |
| **R6: 测试环境差异** | 使用 pytest 而非外部依赖，纯本地执行，无需网络 |

### 风险监控节点

| 检查点 | 时间 | 检查内容 | 行动标准 |
|--------|------|---------|---------|
| 🔍 **Day 1 结束** | 周一 | api_client 测试通过率？ | < 80% 则 Day 2 优先补齐测试 |
| 🔍 **Day 3 结束** | 周三 | 覆盖率 ≥ 60%？ | < 60% 则周五专攻测试覆盖 |
| 🔍 **Day 5 结束** | 周五 | 最终验收 | 覆盖率 ≥ 80% + 0 个 P0 问题 |

---

## 五、📊 成功标准

### 硬性指标（必须达成）

| 指标 | 当前值 | 目标值 |
|------|--------|--------|
| 🧪 测试覆盖率 | 0% | ≥ 80%（核心模块） |
| 🔴 try/except 块数 | 0 个 | ≥ 10 个（覆盖所有 IO 操作） |
| 📝 TODO 数量 | 13 个 | ≤ 3 个 |
| 📦 依赖管理文件 | 无 | requirements.txt + pyproject.toml |
| 🪵 print() 使用 | 1 处 | 0 处（全部替换为 logging） |

### 软性指标（争取达成）

| 指标 | 目标 |
|------|------|
| 📐 代码重复率 | api_client.py 4 次重复 → 1 次统一方法 |
| 📖 异常 docstring | 所有可能抛出的异常在文档中标注 |
| 🚀 管线完整性 | process_data() 一次调用走完全流程 |

---

## 六、📌 每日行动建议

### Day 1 重点
> **"打好地基，不怕楼歪"**
> 先创建 requirements.txt + 测试框架，再开动 api_client 重构

### Day 2 重点
> **"先救命，再治病"**
> api_client 和 data_processor 是最高风险模块，优先完工

### Day 3 重点
> **"让链路跑起来"**
> 管线完整化是用户最直观的体验提升

### Day 4 重点
> **"缝缝补补"**
> 清理小模块的遗留问题和日志

### Day 5 重点
> **"验收封关"**
> 全量测试 + 配置收尾 + 文档记录

---

*本计划基于 note_20260427_154812_0 ~ note_20260427_155222_5 共 6 份分析笔记制定*
*创建时间: 2026-04-27 | 计划版本: v1.0*
